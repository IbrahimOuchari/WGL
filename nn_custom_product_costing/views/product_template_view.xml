<odoo>
    <record id="view_product_template_tree_inherit_bom_cost" model="ir.ui.view">
        <field name="name">product.template.tree.inherit.bom.cost</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <!-- Add your field after the 'type' field (or choose another place) -->
            <xpath expr="//tree/field[@name='type']" position="after">
<field name="bom_cost_set" optional="hide" string="Coût nomenclature défini"/>
<field name="bom_exist" optional="hide" string="Nomeclature Exist"/>
            </xpath>
        </field>
    </record>
</odoo>
